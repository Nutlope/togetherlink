name: Live Agent Gauntlet

on:
  workflow_dispatch:
    inputs:
      harness:
        description: "Harness test file to run"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - claude
          - codex
          - grok
          - live-models-check
          - opencode
          - pi
          - prime

  push:
    branches:
      - main
    paths:
      - ".github/workflows/live-agent-gauntlet.yml"
      - ".oxfmtrc.json"
      - "package.json"
      - "packages/cli/**"
      - "packages/models/**"
      - "packages/tests/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "site/**"

concurrency:
  group: live-agent-gauntlet-${{ github.ref }}
  cancel-in-progress: false

jobs:
  live:
    name: Real inference harness tests
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      CI: "true"
      TOGETHER_API_KEY: ${{ secrets.TOGETHER_API_KEY }}
      TOGETHERLINK_DEBUG: "1"
      VITEST_RETRY: "3"
      HARNESS: ${{ github.event.inputs.harness || 'all' }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Verify Together secret
        run: |
          if [ -z "$TOGETHER_API_KEY" ]; then
            echo "TOGETHER_API_KEY repository secret is required for live inference tests." >&2
            exit 1
          fi

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile --config.confirmModulesPurge=false

      - name: Restore Turborepo cache
        uses: actions/cache/restore@v6
        with:
          path: .turbo/cache
          key: turbo-${{ runner.os }}-${{ github.ref_name }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            turbo-${{ runner.os }}-${{ github.ref_name }}-
            turbo-${{ runner.os }}-

      - name: Verify TypeScript native compiler
        run: node_modules/.bin/tsc --version

      - name: Check formatting
        run: pnpm format:check

      - name: Typecheck workspace
        run: pnpm typecheck

      - name: Run deterministic test groups
        run: pnpm test

      - name: Install coding-agent CLIs
        run: |
          case "$HARNESS" in
            all)
              # OpenCode's official npm package is `opencode-ai`; it installs the
              # `opencode` binary. See https://github.com/anomalyco/opencode.
              npm install -g @anthropic-ai/claude-code @openai/codex opencode-ai@latest @earendil-works/pi-coding-agent
              curl -fsSL https://x.ai/cli/install.sh | bash
              curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh
              echo "$HOME/.grok/bin" >> "$GITHUB_PATH"
              claude --version
              codex --version
              opencode --version
              pi --version
              prime-agent --version
              "$HOME/.grok/bin/grok" --version
              ;;
            live-models-check)
              npm install -g @anthropic-ai/claude-code @openai/codex
              claude --version
              codex --version
              ;;
            claude)
              npm install -g @anthropic-ai/claude-code
              claude --version
              ;;
            codex)
              npm install -g @openai/codex
              codex --version
              ;;
            grok)
              curl -fsSL https://x.ai/cli/install.sh | bash
              echo "$HOME/.grok/bin" >> "$GITHUB_PATH"
              "$HOME/.grok/bin/grok" --version
              ;;
            opencode)
              # OpenCode's official npm package is `opencode-ai`; it installs the
              # `opencode` binary. See https://github.com/anomalyco/opencode.
              npm install -g opencode-ai@latest
              opencode --version
              ;;
            pi)
              npm install -g @earendil-works/pi-coding-agent
              pi --version
              ;;
            prime)
              curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh
              prime-agent --version
              ;;
            *)
              echo "Unknown harness: $HARNESS" >&2
              exit 1
              ;;
          esac

      - name: Build togetherlink CLI
        run: pnpm exec turbo run build --filter @togetherlink/models --filter @togetherlink/cli

      - name: Typecheck live tests
        run: node_modules/.bin/tsc -p packages/tests/tsconfig.json --noEmit

      - name: Run live agent gauntlet
        run: |
          case "$HARNESS" in
            all)
              VITEST_FILE_PARALLELISM=0 VITEST_MAX_CONCURRENCY=1 pnpm test:live
              ;;
            claude)
              TOGETHERLINK_LIVE_TEST_FILE=src/Claude.test.ts pnpm --filter @togetherlink/tests test:live
              ;;
            codex)
              TOGETHERLINK_LIVE_TEST_FILE=src/Codex.test.ts pnpm --filter @togetherlink/tests test:live
              ;;
            grok)
              TOGETHERLINK_LIVE_TEST_FILE=src/Grok.test.ts pnpm --filter @togetherlink/tests test:live
              ;;
            live-models-check)
              TOGETHERLINK_LIVE_MODELS_CHECK=1 TOGETHERLINK_LIVE_TEST_FILE=src/livemodelscheck.test.ts pnpm --filter @togetherlink/tests test:live
              ;;
            opencode)
              TOGETHERLINK_LIVE_TEST_FILE=src/OpenCode.test.ts pnpm --filter @togetherlink/tests test:live
              ;;
            pi)
              TOGETHERLINK_LIVE_TEST_FILE=src/Pi.test.ts pnpm --filter @togetherlink/tests test:live
              ;;
            prime)
              TOGETHERLINK_LIVE_TEST_FILE=src/LivePrimeRlm.test.ts pnpm --filter @togetherlink/tests test:live
              ;;
            *)
              echo "Unknown harness: $HARNESS" >&2
              exit 1
              ;;
          esac

      - name: Stop togetherlink daemon
        if: always()
        run: node packages/cli/dist/bin/togetherlink.js daemon stop || true

      - name: Save Turborepo cache
        if: always()
        uses: actions/cache/save@v6
        with:
          path: .turbo/cache
          key: turbo-${{ runner.os }}-${{ github.ref_name }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Upload live test artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: live-agent-gauntlet-artifacts
          path: packages/tests/artifacts/
          if-no-files-found: ignore
          retention-days: 14
